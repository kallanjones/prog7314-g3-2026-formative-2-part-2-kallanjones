# Keep host build output out of the image — it would clash with the
# build done inside the container.
bin/
obj/

# Local database and developer settings
*.db
*.db-shm
*.db-wal
appsettings.Development.json

Dockerfile
.dockerignore
